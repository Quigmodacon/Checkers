

#include "checkers.hpp"
/////////////////////////////////////////////////
// Board Class
/////////////////////////////////////////////////
void board::board(int size = 8) {
	boardSize = size;
	bool isEven = true;
	for (int col = 0; col < boardSize; col++) {
		for (int row = 0; row < boardSize; col++) {
			
		}
	}
}
